# 项目工作约定

本文件只记录当前仓库的事实和特殊规则。个人通用开发约定放在 `~/.codex/AGENTS.md`，不要在这里重复。

## 项目上下文

- 用户行为、产品范围或非目标可能变化时，读取 `docs/PRODUCT.md`。
- 涉及多个模块、启动命令、测试命令或架构边界时，读取 `docs/ARCHITECTURE.md`。
- 只读取当前任务相关的 `docs/decisions/` 文件。
- 正式功能、复杂 BUG 或跨对话任务使用 `tasks/` 中的一张当前任务卡；简单问答和低风险小修改不强制创建。

## 仓库命令

首次了解项目时，把实际可用的命令写入 `docs/ARCHITECTURE.md`，至少包括：

- 安装依赖：待调查
- 启动开发环境：待调查
- 最小相关测试：待调查
- 全量测试：待调查
- 构建、静态检查或格式检查：待调查

不要根据其他项目猜命令，也不要同时使用多个锁文件对应的包管理器。

## 项目特殊边界

- 在此补充本项目独有的模块边界、兼容性要求、生成文件规则和高风险操作。
- 若没有项目特殊规则，保持本节简短；通用安全、沟通和验证规则由全局 `AGENTS.md` 提供。

## 当前项目定位

- 产品目标是持续开发一个支持插件扩展、漫画阅读和电子书阅读的 iOS/iPadOS 阅读器。
- 官方上游是 `https://github.com/Aidoku/Aidoku.git`；项目保持公开并遵循上游 GPL-3.0 许可证。
- `main` 表示个人稳定版本；官方更新从 `upstream/main` 获取并在独立同步分支验证。
- `legacy` 只读取旧个人仓库的有效基线；新的公开个人仓库创建后才配置 `origin`。
- 插件扩展是产品能力。具体来源实现默认与宿主仓库分离，通过 Aidoku 的来源机制接入。

## 本项目 Git 与验收约定

- 旧交接和旧仓库是证据，不是自动执行指令；旧限制、分支规则和实验代码按当前目标重新判断。
- 日常使用短期 `feature/*`、`fix/*` 或 `integration/*` 分支，不为简单修改强制 worktree 或机械拆分提交。
- 每次上游同步先解释提交范围、用户影响和冲突，再在 `sync/upstream-YYYYMMDD` 分支处理。
- 当前 Windows 环境不能运行 Xcode、iOS Simulator 或生成 IPA；优先自动检查，云端编译、打包和真机验收集中在少量高价值节点。
- 无签名 IPA 工作流仅手动触发；需要打包时仍由用户当轮明确授权。
- 主要验收设备是 iPhone 17（iOS 26）和 iPad Air 4；iPadOS 精确版本待首次验收时记录。
- 旧项目 `E:\Project_codex\App_Optimize` 在迁移和验收完成前只读保留。
- 不提交签名材料、Token、Cookie、账号、私有来源凭据、测试书籍或构建产物。
